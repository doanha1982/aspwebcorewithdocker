# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

variables:
  buildConfiguration: 'Release'
  dockerId: 'doanmanhsonha'
  pswd: 'nguyenthithanhha'
  imageName: 'aspnetcorewithdocker'

steps:
- script:
    docker login -u doanha1982 -p nguyenthithanhha
    docker-compose -f docker-compose.yml --project-directory . -p AspWebCoreWithDocker 
    docker push doanha1982/aspnetcorewithdocker

